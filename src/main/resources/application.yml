server:
  port: 80
  error:
    include-message: always
    include-binding-errors: always
spring:
  datasource:
    password: "postgres"
    url: "jdbc:postgresql://postgres.canvkzdh042c.eu-central-1.rds.amazonaws.com:5432/graduationproject"
    username: "postgres"
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL81Dialect
        format_sql: true
    show_sql: true
ecommerce:
  app:
    jwtCookieName: graduationproject
    jwtSecret: "jwt_secret"
    jwtExpirationMs: 86400000